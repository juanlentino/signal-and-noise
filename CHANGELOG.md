# Changelog

All notable changes to the Signal & Noise theme are documented here.

This file holds two things only: **`## [Unreleased]`**, the working log that
accumulates across pull requests, and the **current release**. Everything older
lives in [docs/changelog/](docs/changelog/).

A pull request does not bump `Version` and does not tag — it closes an issue and
adds a bullet below. A release is a separate, deliberate act:
`tools/cut-release.sh`, which stamps `style.css` **and** `readme.txt` together.

## [Unreleased]

### Fixed
- **The header logo renders again, and it no longer depends on an upload.** `parts/header.html` hardcoded `<img src="/wp-content/uploads/2026/09/cropped-jl_logo-min-150x150.webp">`, a specific attachment thumbnail; the file stopped resolving (`naturalWidth === 0`) and nothing in the theme, in wp-admin, or in any cache purge could repair a string pointing at a missing upload. The header now carries the JL mark inline as SVG (`viewBox 0 0 146 128`, stroke 20, `stroke="currentColor"`) inside the home link (`aria-label="Juan Lentino — home"`, the SVG `aria-hidden`), so there is no logo request at all. The mark is inked with `bone`, the token the dark layer already flips, so one declaration covers both schemes and the `--sn-mark-invert` filter token and its `invert()` rule are gone with the raster. It keeps the exact heights the image had (80/48, 56/38, 44/34 across the header states and breakpoints, all above the 16px floor) so the header height, and `body` padding-top, `scroll-padding-top` and `article-toc.js`'s offsets keyed to it, do not move; width follows the viewBox and is never set alone. Pinned in `tests/brand-mark.php` (12): inline SVG with the canonical geometry, one accessible name, no `<img>` or uploads URL, zero `cropped-jl_logo` references across the theme, every `.jl-mark` height at or above 16px, no fixed width, the `bone` ink and no second dark rule; 6 red against the old part.

### Changed
- **One icon set, from `assets/brand/`.** `inc/dark-mode.php` emits `favicon.svg` (the JL tile; it carries its own `prefers-color-scheme` rule and inverts with the OS), `favicon.ico` (`sizes="any"` fallback) and one opaque 180px `apple-touch-icon.png`, in that order, and the light/dark PNG pairs (`favicon-32*`, `app-icon-180*`) go with the `jl-logo-*` and `app-icon-192/512` rasters nothing referenced. Core's `site_icon_meta_tags` are now removed wholesale (`__return_empty_array`) rather than only the apple-touch-icon: `wp_site_icon()` runs at `wp_head:99`, after the theme's block at 1, and with `icon-512.png` set as the Site Icon it would have printed a second, competing favicon set; the uploaded Site Icon still serves wp-admin, the login screen and the manifest, which read it directly. `assets/js/dark-mode-toggle.js` stops rewriting favicon `href`s on toggle: the SVG inverts by itself, and the old pair-swap run against the new head wrote the `.ico`'s href onto the SVG link (`tests/dark-mode-chrome-sync.php` goes red on the old script for exactly that). The reader-toggle-against-OS case now updates `theme-color` only; the tab icon follows the OS, which the brief accepts. `tests/head-sweep.php` pins the single-set shape and the SVG-before-ico order; `tests/home-screen-icon-opacity.php` pins both brand PNGs opaque (colour type 2) and the wholesale filter (red on `__return_false`).
- **The site-default `og:image` is the 1200x630 JL card.** The companion plugin seeds `sn_og_image_url` with the wp-admin "Default OG image URL" setting (a small square logo); `inc/notes-og-card.php` now replaces that seed at priority 5 with `assets/brand/og-image.png`, before the plugin's per-post cards (10) and the `/notes` card (20), so singular views are unchanged and home, archives and search preview with the card. The plugin already declares `twitter:card=summary_large_image` and `twitter:image` from the same value. `og-image.png` was re-rendered headlessly from `og-image.html` with the theme's own `dm-mono-400/500-latin.woff2` (the shipped PNG used a substitute monospace); the template's `@font-face` points at `assets/fonts/` and documents the render command. `tests/notes-og-card.php` (+4) and `tests/cross-package-listeners.php` Contract 7 pin the new default.
- **Buttons are square.** `theme.json` `styles.elements.button.border.radius` was `999px`, a pill, against the README's "no rounded corners"; it is `0`. The sweep of `assets/css`, `blocks/`, `patterns/` and `styles/` found four other non-zero radii and left all of them, each deliberate: `50%` on three true circles (`.sn-disco-play-circle`, the 7px `.sn-music-featured__dot` and `.sn-availability__dot`) and `--sn-spotify-radius: 12px`, documented in place as matching Spotify's own embed chrome.

## [13.4.1] - 2026-09-20 — the theme speaks core

### Fixed
- **A note with the companion plugin absent renders its `<main>` again.** `inc/reading-path-slot.php`'s `render_block` bridge returned `''` for any block whose content carried `[sn_reading_path]` with the shortcode unregistered. Inner blocks render first, so the parent `core/group` (the `<main class="sn-note-single">` of `templates/single.html`) reached the filter with the token still inside its 6,405 characters and was blanked whole: header, footer, no article (measured on WordPress 7.1). The bridge now strips the token, with its `<p>` wrapper, and blanks only a block that held nothing else, which is the slot. `tests/reading-path-slot.php` feeds a parent group with the shortcode unregistered and pins that the surrounding markup survives (8, was 6; the new pin fails against the old bridge).
- **The theme's own Copilot tool-schema normalizer was a hand-rolled twin of OpenStation's `openstation_ai_normalize_tool_schema()`, registered on a hook name OpenStation dropped at v1.0.0.** `inc/desktop-mode-copilot-schema.php` hooked `desktop_mode_ai_tools` alone, so on every release the site has run since August it never fired, while its docblock and the `functions.php` require line promised a guard the theme did not provide. Read: OpenStation's `docs/hooks-reference.md` names `openstation_ai_tools` as the Stable tool-list filter, and `includes/ai-copilot/search.php` at v1.1.10 normalizes every tool schema itself after that filter (the union top-level `type`, top-level `anyOf`/`oneOf`/`allOf`, empty `properties`, idempotent), which is the converter the module's docblock said should own this. Upstream has done this since v0.9.6 (2026-07-23, upstream #366, the fix for #362; the `openstation_` name since v1.0.0), one week after the theme's module was born in 10.42.3 citing that same #362, so the module was redundant on every release but the first week and dead by name on every release since August. The module, its require line, its file-map row and `tests/desktop-mode-copilot-schema.php` are deleted; the three shapes the theme declares are untouched and now pinned in `tests/abilities-registration.php` (+3: the top-level `anyOf`, the `object|null` union type, the empty `properties`). Nothing on screen changes: the module filtered an admin request the Copilot builds, never public output, and on the live site (OpenStation 1.1.10, companion plugin active) it was not running at all. Pinned by derivation in `tests/openstation-hook-names.php` (7): every hook name the theme registers or fires is read off the source with `token_get_all()` (a regex stripper opened a comment on the `styles/*.json` inside a description string and swallowed 225 lines of `inc/abilities-diagnostics.php`; found in review), a constant first argument resolves through the theme's own `const` and `define()` lines, none may start with `desktop_mode_`, and every `require_once` in `functions.php` must resolve; red against 13.4.0 both ways. #382.

### Changed
- **Five regex rewrites of rendered HTML are now four reads and writes on `WP_HTML_Tag_Processor`, and the page-wide generator buffer is gone.** The TOC (`inc/article-toc.php`), the note-link previews (`inc/note-link-previews.php`), the view-transition name (`inc/blocks-view-transitions.php`) and the Spotify oEmbed filter (`inc/frontend-filters.php`) each found a tag with a regex over the rendered markup and rewrote it by string splice or `substr_replace`; core's HTML API, stable since 6.2 and the plugin's own rewrite tool, does that job without a null return and without re-parsing a tag by hand. Read: `class-wp-html-tag-processor.php` on the `wordpress-develop` 7.1 branch, `set_attribute()` (places a NEW attribute right after the tag name, encodes the value itself, runs `src`/`href` through `esc_url()`), `set_bookmark()`/`seek()` (the TOC reads a heading's text before it can slug it), `get_modifiable_text()` and `WP_HTML_Decoder::decode_text_node()` (decoded once, so a texturized excerpt's `&#8217;` is never doubled). What a browser builds is unchanged on every fixture: same heading ids and anchors (the processor hands the heading text back decoded, and core's `sanitize_title_with_dashes()` `strip_tags()`es a decoded `<meta>` out of a heading that spells one, so the label is re-escaped with `esc_html()` before it is slugged and `The &lt;meta&gt; tag` keeps its `#the-meta-tag`; found in review, no live H2 carries one today), same three preview stamps, same `view-transition-name`, same dark square Spotify embed. The bytes moved in three places, all browser-identical: a generated heading id now leads its class (`<h2 id="intro" class="wp-block-heading">`), the preview stamp sits before `href` (in the processor's order, meta, summary, title), and the Spotify `src` serialises `&#038;theme=0` where it wrote `&theme=0`. The view-transition port is byte-identical. The output buffer behind `sn_strip_generator_meta()` stripped nothing (core's tag already goes through `remove_action` + `the_generator`, no active plugin emits one, `/notes/` never ran it) and was the one callback that could ship an empty 200, which `/provenance/` suffered twice (13.3.1); it went, with `sn_generator_meta_buffer_start()`, `sn_emit_header()`, `SN_PAGE_BODY_FLOOR_BYTES` and the `template_redirect` wiring. The badge splice in `inc/provenance-title-badge.php` stays: the API has no insert. Pinned: `tests/lib/wp-html-api.php` loads the API (`SNT_WP_HTML_API`, or a normal install) and ends a suite RED when it cannot, and `ci.yml` now fetches the seven files for the `tests` and `php-matrix` jobs; origin/main's bytes for every fixture are captured in the suites and compared as decoded shape (16 pins go red against the old modules, and 4 more against the unfixed port for the escaped heading: the source greps, the attribute placements, the `&#038;` byte and the buffer's absence). #383.
- **The footer meta-nav is a named block, not a Custom HTML block.** The five icon links (Now, Accessibility, Colophon, Stats, Privacy) rode one `wp:html` block in `parts/footer.html` because the paragraph block strips SVG from rich text; they are now the `signal-noise/meta-nav` PHP-only block (`blocks/meta-nav/`, the tenth slug in `inc/blocks-php-only.php`, `supports.autoRegister`, no JavaScript), "Meta nav" in the site editor's List View and movable as one unit. Read against the installed 7.1 core before choosing the shape: the Icons API registry sanitizes every icon through a fixed allowlist (`svg`, `path`, `polygon`; no stroke attribute anywhere, no `circle`), so the five stroked marks registered as they are come back as unfilled paths with the clock face and the figure's head gone, and `wp_get_icon()` is a fatal at the theme's 7.0 floor (the function is `@since 7.1.0`). So the glyphs stay literals in `render.php`, byte for byte what the `wp:html` block carried, and the registry port (a redraw as filled paths with a pixel acceptance, and the floor or a guard) waits on the owner. Pinned: `tests/fixtures/footer-meta-nav.html` is the 13.4.0 block's inner content captured through `WP_Block_Parser` (2412 bytes, sha256 pinned); `tests/footer-meta-nav.php` (42) and `tests/blocks-php-only.php` (68) assert the render equals it, the part places the block once and carries no `<svg>`, and the block is in the palette allowlist. Red against 13.4.0: 3 on the part, 6 on the module, 17 on the missing block, 1 on the palette. #388.
- **The nine dead `render_block` shortcode bridges are gone and the footer year is a Block Binding.** Nine modules each kept a global `add_filter( 'render_block', ..., 10, 2 )` running `strpos()` on every rendered block to re-resolve a `[shortcode]` token, "belt-and-suspenders" since 9.9.1. Read against the 7.1 branch: core runs `do_shortcode()` on a template's and a part's raw markup before `do_blocks()` (`block-template.php`, `_wp_apply_block_content_filters()`) and `render_block_core_shortcode()` is `wpautop()` and nothing else, so the seven template-path bridges never found a token on the front end, and their tokens left the templates for PHP-only blocks in 13.1.0. Post content runs the other way round, `do_blocks` at `the_content` priority 9 and `do_shortcode` at 11 (`default-filters.php`), so the two post-content bridges (`[sn_email]` on /contact, `[sn_build]` on /colophon) resolved their token two filters before `do_shortcode` would have, with the same bytes; the live /contact and /colophon bodies carry neither token today (curl 2026-09-20), so the page output is unchanged. The footer's inline `[current_year]` becomes a `signal-noise/site-field` binding (`register_block_bindings_source`, Block Bindings API) with one key, `copyright`, the whole line, since a paragraph binding replaces the block's entire content; the year still comes from `signal_noise_current_year()` (pinned with the helper and `wp_date()` stubbed to different years, so a bypass reads red), and the `<p>` keeps `© Juan Lentino` as the fallback the Site Editor canvas shows behind the binding lock. The shortcodes stay registered for post content (owner decision 2026-09-12). Two bridges survive on purpose and are named: `inc/404-recovery.php` until #386 places the 404 block, and `inc/reading-path-slot.php`, whose plugin-absent guard is load-bearing until the plugin ships `signal-noise/reading-path`. Pinned: `tests/block-bindings.php` renders the bound footer paragraph the way `WP_Block::replace_html()` does in 7.1 and matches it byte for byte against the old line captured from 13.4.0's `parts/footer.html`; `tests/render-block-bridges.php` names the two survivors and the only tokens left in template markup; `tests/block-bindings-templates.php` admits the site-field slot; the bridge cases in six suites are now absence pins. `docs/WORDPRESS-REFERENCE.md` 1.2 rewritten. #389.
- **The 404 suggestions list is a block; the last theme-owned `core/shortcode` slot is gone.** `templates/404.html` kept `[sn_404_suggestions]` in a Shortcode block, the one furniture token the 13.1.0 port left out, so the Site Editor showed a literal token where the nine siblings show a name. Read: WordPress 7.1 resolves shortcodes on the raw template markup before `do_blocks()` (`wp-includes/block-template.php`, `get_the_block_template_html()`), and `render_block_core_shortcode()` is `wpautop( $content )` and nothing else; `nav` is in `wpautop()`'s block list, so the `<p>` the module's `render_block` bridge claimed to strip never existed and the token never reached the bridge unresolved. Changed: `blocks/suggestions-404` (`block.json` with `supports.autoRegister`, core since 7.0; `render.php` calling `sn_404_suggestions_shortcode()` through `sn_php_block_output()`, the same `wpautop()` wrapper as the nine), the slug in `sn_php_only_block_slugs()` and the editor palette, `<!-- wp:signal-noise/suggestions-404 /-->` in the template (a block name must start with a letter: core's parser and the JS registration both refuse `404-suggestions`, and `tests/blocks-registry.php` now pins every manifest name against that rule), and the bridge removed from `inc/404-recovery.php`. The shortcode stays registered for post content, as the nine do (owner decision 2026-09-12). Pinned: `tests/404-recovery.php` runs core's real `wpautop()` (`tests/lib/wp-autop.php`, copied verbatim from 7.1) over the block's render and compares it with the bytes the old template path produced, captured from the pre-swap code, byte for byte; `tests/blocks-php-only.php` counts ten blocks, the parity pair, the template placement, no `wp:shortcode` left in `404.html`, the palette. Fourteen assertions red against 13.4.0, green after. #386.
- **Three block view scripts loaded by a page gate that repeated the renderer's predicate; they load from `block.json` `viewScript` now.** `blocks/note-share`, `blocks/note-reply` and `blocks/theme-toggle` declared no script; `inc/assets-frontend.php` and `inc/note-reply.php` each enqueued theirs behind a second `is_singular( 'post' )` and `inc/dark-mode.php` enqueued the toggle's on every page, so the dependency lived outside the manifest and the predicate was written twice. Read: WP 7.1 `wp-includes/blocks.php` `register_block_script_handle()` returns a bare handle verbatim (the shape `blocks/sidenote` already uses for `editorScript`), and `wp-includes/class-wp-block.php` `render()` enqueues every `view_script_handles` entry when the block renders, before and regardless of what the render callback returns; `template-canvas.php` renders the template before `wp_head()`, so a footer-registered handle still prints in the footer. Changed: each manifest names its handle (`sn-note-share`, `sn-contact-aliases`, `sn-dark-mode-toggle`), each module registers that handle at `init` with the arguments its enqueue used to pass, and the three `wp_enqueue_scripts` gates are gone; the `/contact` enqueue of the shared alias handle stays, that page is not a block. The rendered block output is untouched and the printed `<script>` tags carry the same src, version and defer/footer arguments; two things in the served page differ. Footer order: on notes the three now precede the handles `wp_enqueue_scripts` adds, all of them independent deferred files. And reach: `templates/single.html` is the only include of `parts/post-closing.html` and there is no `attachment.html`, so every singular that falls to `single.html` without being a `post` (attachment pages today) prints `note-share.js` and `contact-aliases.js` where the old `is_singular( 'post' )` gates did not; both scripts querySelectorAll their row and no-op without it, so the page behaves as before, and whether attachment pages stay enabled (`wp_attachment_pages_enabled`) is the owner's question, not this cut's. The Interactivity API rewrite of the toggle's state is the issue's named second step and is not in this cut: it changes the toggle's public markup (directives, and a server state block, since the server-side directive processor removes a `data-wp-bind` attribute and blanks a `data-wp-text` that evaluate to null), and three harnesses still run `dark-mode-toggle.js` as a bare script. `tests/block-view-scripts.php` (35) pins the manifests, the registrations against origin/main's captured enqueue arguments, that no gate enqueues the three handles on a single note, with a sibling gated handle and the `/contact` enqueue as controls, and the template shape the attachment reading rests on; 26 red against the unfixed code. #384.
- **Five hand-written `clamp()` presets measured against core's `fluid: { min, max }`, and the three inert opt-outs removed.** `theme.json` sets `typography.fluid` on and then hand-writes `clamp(min, Xvw, max)` on `xx-large`, `xxx-large`, `display-sm`, `display-md` and `display-lg`; the native shape is a bare size plus a `fluid` object, computed by core from one global viewport pair. Read `wp_get_typography_font_size_value()` and `wp_get_computed_fluid_typography_value()` in WordPress 7.1 and drove them, not a re-derivation: core draws a line from `(minViewportWidth, min)` to `(maxViewportWidth, max)`, a hand-written `Xvw` is a line through the origin, and the two coincide only at that preset's own floor and cap crossing, which differs for each of the five. Under today's effective pair (320px to 1400px) every one of the five renders differently at 768 and 1024, and any other pair reproduces at most one of them while rewriting the seven bare presets core already fluidises. So none ports and all five stay literal; the pair is the owner's call (#387 names it). What changed: the `"fluid": false` beside the three `display-*` clamps was inert (core returns a `clamp()` size verbatim either way) and is gone, byte-identical output. Pinned in `tests/fluid-type-presets.php` (73): a verbatim copy of core's `block-supports/typography.php` as the fixture, the seven bare presets equal to the strings the live site emitted on 2026-09-20, the exact `clamp()` core would emit for each of the five with the px at 320, 768, 1024 and 1440 beside the hand-written line, and the two own-crossing pairs proving one preset per pair. A red on one "differs" line is a crossing at that width, not a match; the port signal is the per-preset "every tested width" line, red when a port would render the same px at 320, 768, 1024 and 1440 (never byte-identical: core's middle term is `min + ((1vw - offset) * factor)`, the hand line is `Xvw`). #387.
- **The sidenote block ships its own sheet through block.json `style`; its rules rode `assets/css/article.css` into the combined stylesheet on every page.** Read on the 7.1 branch: `register_block_style_handle()` registers the file and stores its path as style data, `WP_Block::render()` enqueues the handle where the block renders, `wp_maybe_inline_styles()` inlines it in the head under the 40 KB budget, and the iframed canvas enqueues every registered block sheet on `enqueue_block_assets`; a block theme sets `should_load_block_assets_on_demand`, so a page with no sidenote never carries it. The live `/provenance/` head already prints the pillar-essays sheet that way. `article.css:612-649` moved verbatim to `blocks/sidenote/style.css`, `"style": "file:./style.css"` in the manifest, `patterns/sidenote.php` inserts the block instead of a bare paragraph the sheet would no longer reach (no live page used the paragraph form), and the renderer's pointer is `./style.css`. 71 of 74 sitemap pages drop the rules from the combined sheet; the three with a sidenote get them inlined. Pinned in `tests/sidenote-scope.php`: the manifest declares the sheet, no source of the combined sheet carries `.sn-sidenote`, and the block sheet minifies to the exact bytes `article.css` contributed on `origin/main` (14 red against the unfixed code across it and `tests/blocks-registry.php`). The pull-quote half waits on the pattern's retirement: one live Note uses the pattern, not the block, and a block sheet would strip its frame. #391.
- **Four block styles are theme.json partials, not PHP strings the Site Editor could not see.** `inc/block-styles.php` registered Hairline (`core/separator`), Signal and Epigraph (`core/quote`) and References (`core/list`) with `register_block_style()` and a hand-concatenated `inline_style` each; core appended the string to the block's own stylesheet and never parsed it, so Styles > Blocks, the global styles REST route and the schema were all blind to them, while Eyebrow, Eyebrow (Large) and Caption already used the 6.6 partial form. Read on the 7.1 branch: `WP_Theme_JSON_Resolver::get_style_variations( 'block' )` and `wp_register_block_style_variations_from_theme_json_partials()`, `process_blocks_custom_css()` (takes `!important` as written, turns `& li{}` into `:root :where(sel li){}`, and DROPS any part with nested braces, so an at-rule inside a variation emits nothing), and `wp_render_block_style_variation_support_styles()`, which is how a variation with `elements` reaches the front end (scoped per instance, printed after global-styles and before the theme's sheets). The 12.6.0 note that JSON "provably cannot express them" was wrong on both counts: `css` carries the `!important` and the descendant, and `cite` is an element a variation may style. Each is now `styles/blocks/<slug>.json`: structured keys for what the schema names (`border`, `color`, `spacing`, `typography`, `dimensions`, `elements.cite`) and `css` for the rest (`opacity`, `max-width`, `list-style`, the `& li` descendants, `display:block` on the cite, the Epigraph's `margin-left:0 !important`, which still has to beat the constrained layout's `margin-left:auto !important` at the same 0-1-0). The PHP file and its `require_once` are gone; the seven partials pin `$schema` to `wp/7.1` like `theme.json` instead of `trunk`. The specificity drop (0-2-0 and 0-3-0 in the block sheet to 0-1-0 after global-styles) exposed one real fight: `components.css` `.wp-block-separator { border-color: concrete !important; opacity: 0.6 }` prints after the variation and would have faded the Hairline to 0.6. That base rule now lives where the separator's width and text color already did, `theme.json` `styles.blocks.core/separator` (`border.color`, `css: "opacity:0.6"`), printed in global-styles before any variation, and the `!important` on it, on the Hairline and on `article.css` `.sn-post-closing__rule` (#320) all go. The References print rule (`break-inside`, the rust URL after each link) moves to `assets/css/print.css` under the same `media="print"` enqueue. No live page changes: every live separator is one of three shapes with no inline colour, the four live Notes with References keep their markup and computed values, and nothing live uses the other three. Pinned: `tests/block-styles.php` (94) compiles each partial the way core does and holds the longhand declaration map equal to origin/main's `inline_style` strings per selector (the two named exceptions asserted on their own), sweeps every screen rule in `assets/css/` for a later re-paint at 0-1-0 or above, and pins the base rule; `tests/editor-variations-parity.php` derives the styles side from the partials (the three typography partials stay row-less, pinned as the declared set); `tests/template-typography.php` lists the four in its map and governs their one-off literals there; `tests/editorial-conventions.php` and `tests/post-closing-rule-color.php` re-pinned. 44 assertions red against the unfixed code. #390.
- **Core inlines the navigation stylesheet; the theme stops copying it and stops deferring a sheet core never links.** Two hand-rolls, one from March 2026 and one from July, sat on one symptom: `assets/css/critical.css` carried a verbatim copy of core's closed-state navigation rules (10.38.1, written against a nav request intermittently poisoned at the Cloudflare edge), and `inc/assets-frontend.php` rewrote the `wp-block-library` and `trp-language-switcher` link tags to `media='print' onload` through `style_loader_tag`, which core never reaches for either on a block theme (`wp-block-library` is registered with a src on a block theme, but core inlines it first: `wp_maybe_inline_styles()` runs at `wp_head` priority 1 and sets `src` to false on every sheet under the budget, so by the time `wp_print_styles` runs at priority 8, `WP_Styles::do_item()` returns at `if ( ! $src )` before the filter; live, `common.min.css` is 4,203 bytes and prints as `<style id="wp-block-library-inline-css">`; TranslatePress is gone from the site). Read `wp_maybe_inline_styles()` and the documented `styles_inline_size_limit` filter (core 5.8, default 40,000 bytes since 6.9): core inlines each block's sheet in `<head>`, smallest first, and breaks at the first overflow. Measured live on 2026-09-20: the home page inlines 23,363 bytes of block CSS, a Note 21,580, and the navigation sheet is 20,776, so it was the one sheet over the budget and the one that shipped as a render-blocking `<link>`. The theme now sets the budget to 64 KB with one filter line; every measured page inlines the nav, there is no nav request left for the edge to drop, and the copy (3,047 bytes off every page's inline critical block) and the dead deferral are deleted. Every declaration the copy carried was checked against core's live 7.1.1 sheet: all 23 are there. The one-line `color: inherit` on nav links (10.38.2) and the is-menu-open overlay (8.5.7) stay. `tests/styles-inline-size-limit.php` (12) runs the real registration through stubbed `add_filter`, calls the callback with core's default and pins the arithmetic against the measured sizes; 8 red against 13.4.0. `tests/nav-responsive-fallback.php` retires with the copy; `tests/cf7-removal.php` now pins that no `style_loader_tag` filter is registered at all, red on the old file. #385.

