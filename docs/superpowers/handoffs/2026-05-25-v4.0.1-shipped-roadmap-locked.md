# Handoff — 2026-05-25 (v4.0.1 shipped, v4.0.x roadmap locked)

**Hand off because:** post-v4.0.0 cost audit + v4.0.1 efficiency patch + v4.0.x sequencing roadmap all landed in one continuing session. Clean break point. Next session can pick up v4.0.2 execution from the roadmap doc.

---

## TL;DR — production state

| Surface | Latest tag | Patch cap | Minor cap |
|---|---|---|---|
| **Theme** | **v9.1.5** | 5/7 in v9.1.x | 1/5 in v9.x |
| **Plugin** | **v4.0.1** | 1/7 in v4.0.x | 1/5 in v4.x |

User confirmed v4.0.0 working in production earlier today via WP-UI Updates install. **v4.0.1 not yet deployed** — user will install via wp-admin Updates UI when ready.

**Ships this continuation:**

| # | What | Commit |
|---|---|---|
| 1 | **plugin v4.0.1** — Cache AI drift verdicts per `(post_id, post_modified, md5(prompt))` to stop redundant re-evaluation on Run scan. Surfaced from production AI Request Logs audit. 3 files, +43/-10 LOC. | `890d43d` / tag `v4.0.1` |
| 2 | **plugin docs** — `docs(roadmap): v4.0.x patch sequence` — locks sequencing + scope boundaries for v4.0.2 + v4.0.3 | `a8c2e7c` |

---

## What's outstanding (for next session)

The full v4.0.x roadmap is in [`signal-and-noise-tools/docs/superpowers/specs/2026-05-25-v4.0.x-roadmap.md`](`/Users/juanlentino/Projects/signal-and-noise-tools/docs/superpowers/specs/2026-05-25-v4.0.x-roadmap.md`). Summary:

### v4.0.2 — Inline-`<img>` alt Suggest + Copy (NEXT)

Closes v4.0.0 CHANGELOG-flagged scope debt. Recommended impl per roadmap: **Option B** (sibling ability `signal-noise/ai-alt-inline-suggest` with separate `(post_id, image_src)` schema). ~120 LOC. Apply remains deferred indefinitely per block-serialization risk.

### v4.0.3 — Before/After Apply preview modal

Replaces `window.confirm()` with side-by-side diff modal. **Zero new AI cost** (UX layer on top of existing Apply flow). ~150 LOC. Benefits both v4.0.0 features + v4.0.2 inline-img.

### Sequencing rationale (locked in roadmap)

v4.0.2 first (discharges public scope debt), THEN v4.0.3 (so modal benefits both v4.0.0 and v4.0.2 features). Each ships as its own focused patch per project's v3.8.x convention.

### Deferred to v4.1.0+

- Stale-posts + orphaned-media Suggest+Apply (original v4.0.0 brainstorm scope)
- Color drift Health check (zero AI, cheap candidate)
- Block pattern usage analytics (zero AI, cheap candidate)
- Inline-img alt **Apply** path (indefinitely deferred pending block-serialization primitive)

### Standing item

- **GSC sitemap submission** — `/wp-sitemap.xml` verified fetchable; user needs to submit in Google Search Console (15 min, no code).

---

## Process highlights (this continuation)

- **Cost audit caught the redundancy before adding more AI features.** User flagged repetition in production AI Request Logs → diagnosed 8-10 redundant drift evaluations per scan → v4.0.1 ships the cache. Saved ~80% of v4.0.0's recurring AI cost on stable content.
- **Cost-aware roadmap re-ranking.** Original brainstorm picked AI Copilot (B1) as top leverage; cost audit re-ranked it to tier-4 (high cost) and elevated zero-AI items (Before/After modal, color drift check, pattern analytics) to tier-1.
- **Roadmap doc as a sequencing artifact** distinct from specs/plans. Per-ship specs + plans get written at execution time via the standard skill chain. Roadmap locks scope boundaries between ships so execution-time brainstorms don't re-litigate the sequence.
- **v4.0.1 shipped via subagent-driven-development** — 1 implementer + 1 combined reviewer pass (spec + quality merged because the change is small + verbatim-spec'd). Controller-direct commit + tag + push. ~30 minutes total elapsed.

---

## Key files (for fresh-session reload)

| What | Path |
|---|---|
| **This handoff** | `signal-and-noise/docs/superpowers/handoffs/2026-05-25-v4.0.1-shipped-roadmap-locked.md` |
| Prior handoff | `signal-and-noise/docs/superpowers/handoffs/2026-05-25-v4.0.0-shipped.md` |
| **v4.0.x roadmap (load me first for execution)** | `signal-and-noise-tools/docs/superpowers/specs/2026-05-25-v4.0.x-roadmap.md` |
| v4.0.0 spec | `signal-and-noise-tools/docs/superpowers/specs/2026-05-25-v4.0.0-ai-health-suggest-apply-design.md` |
| v4.0.0 plan | `signal-and-noise-tools/docs/superpowers/plans/2026-05-25-v4.0.0-ai-health-suggest-apply.md` |
| Drift cache impl | `signal-and-noise-tools/inc/health-checks.php` (lines ~541-570 — the cache wrap inside `sn_health_check_drift_time_phrases()`) |

---

## Recommended next-session sequence

1. **Read this handoff + the v4.0.x roadmap doc**
2. **Confirm v4.0.1 is live in production** (1 SSH command):
   ```bash
   ssh -i ~/.ssh/cloudways_deploy master_syguxtyfsh@157.245.116.64 \
     'php -r "include \"/home/master/applications/nffqxsrgxz/public_html/wp-load.php\"; echo SNT_VERSION;"'
   ```
   Should return `4.0.1` (after user installs via WP-UI Updates).
3. **Pick: v4.0.2 (inline-img Suggest+Copy) OR v4.0.3 (Before/After modal)?** Roadmap recommends v4.0.2 first; flagged in §"Sequencing rationale."
4. **Full skill chain for chosen patch**: brainstorming → writing-plans → executing-plans → subagent-driven-development. The v4.0.0 plan is the template for shape; v4.0.x patches are smaller scope (~120-150 LOC each instead of ~330).

---

## Open questions for next session

1. **v4.0.2 Option A vs Option B confirmation** — roadmap recommends B (sibling ability) but brainstorm phase at execution time may revisit
2. **v4.0.3 modal design** — focus trap implementation choice + mobile layout decision deferred to execution-time brainstorm
3. **v4.0.1 verification** — did the cache actually reduce production AI calls? After enough time has passed (user runs at least 2 Health scans on the same content), check AI Request Logs again; should show the second scan with ~0 drift calls instead of ~8-10

---

## One-line summary

**v4.0.1 shipped (`890d43d` / tag `v4.0.1`) caching AI drift verdicts to stop redundant re-evaluation flagged in production logs. v4.0.x roadmap locked: v4.0.2 = inline-img alt Suggest+Copy (Option B sibling ability, ~120 LOC); v4.0.3 = Before/After Apply modal (zero AI cost, ~150 LOC). Per-ship specs + plans written at execution time. v4.1.0 reserved for stale-posts + orphaned-media + cheap zero-AI Health checks.**
