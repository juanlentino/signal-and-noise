# Handoff — plugin v4.10.0 + v4.10.1 SHIPPED (Track B1 complete) (2026-06-07)

**Read this first.** Resumed the [upgrade-opportunities roadmap](../specs/2026-06-06-upgrade-opportunities-roadmap.md) (locked A→B→C, no majors this pass). Built **plugin v4.10.0** (Track B bundle 2, the planned `2026-06-06-v4.10.0-webhooks-privacy-perf.md`), adversarially reviewed it, fixed the confirmed findings, and shipped. **Track A and Track B1 are now fully done.**

## ⭐ TOP: what shipped + current state
- **Plugin `origin/main` = `26905b6` = v4.10.1.** Both tags pushed: **`v4.10.0`** (`2ec2018`, 6 features) + **`v4.10.1`** (`26905b6`, the 6 features + 4 post-review fixes). Neither auto-deploys.
- **v4.10.0 (6 features):** multi-event webhooks (`post.updated`/`unpublished`/`deleted` + per-webhook event checkboxes), `list-abilities` meta-ability (→**42 abilities**), audit-log CSV/JSON export (admin download + ability), Privacy exporter/eraser + suggested Privacy Policy text, opt-in Speculation Rules (prerender/moderate).
- **v4.10.1 (4 fixes from the adversarial review):**
  - **F1 (HIGH)** webhook `post.deleted` was double-firing on trash→empty-trash (`before_delete_post` re-fired on the already-`trash` row; the `publish→trash` transition already fired it — different `delivery_id`s, no dedupe) **and** firing for never-published/auto-draft rows. Fix: `sn_webhook_on_delete` gates on `post_status === 'publish'`. **This is the default WP deletion path (`EMPTY_TRASH_DAYS=30`), not an edge case.**
  - **F2 (MED false-green)** the `WP_Ability` test stub returned `rest_forbidden`/403; real `WP_Ability::execute()` returns `ability_invalid_permissions` (and `ability_invalid_input` for bad input). Stub + all denial/invalid-input assertions made faithful (verified vs abilities-api trunk). Test-only.
  - **F3 (LOW security)** CSV export now neutralizes spreadsheet formula injection (usernames starting with `= + - @` / tab / CR → single-quote prefix).
  - **F4 (cosmetic)** privacy module → canonical `signal-noise-tools` i18n domain.
- **Suite: 50 suites / 1612 assertions / 0 failures; phpcs clean** (falsified — note phpcs `parallel=8` shows "8/8" = **batches not files**; it scans all 91 files. See `[[reference_phpcs_parallel_batches_not_files]]`).
- **The user split the release** mid-session (bumped to 4.10.1, wrote the `[4.10.1]` CHANGELOG headline, polished the lifecycle test). Confirmed via AskUserQuestion: add the security fix + cleanup to the `[4.10.1]` entry, tag **both** v4.10.0 and v4.10.1.

## ⚠ Pending installs (all shipped+tagged, NOT auto-deployed — wp-admin → Updates)
Plugin: **v4.6.0, v4.7.0, v4.8.0, v4.8.1, v4.9.0, v4.10.0, v4.10.1** (confirm which the user has actually installed; v4.10.0/v4.10.1 are brand-new — installing v4.10.1 gets everything). Theme: **v9.9.0**.

## How it was built (the cycle that works — reuse it)
1. **Build workflow** (background): 5 sequential TDD implementers (no parallel — shared bootstrap requires + `inc/settings.php` + the ability-count docblock) + a release-commit agent + a gate agent (full sweep + lint). 6 commits-per-item.
2. **Re-verify the gate myself** (don't trust it). The phpcs "8/8" looked like the documented `.claude`-exclude false-green; a deliberate injection proved coverage was real.
3. **Review workflow** (background): per-task spec + adversarial behavioral review (read real first-party + WP-core source; hunt false-green stubs) → refute-by-default verify of every MEDIUM+ finding (3 lenses: correctness / real-contract / security-edge; confirmed only if ≥2/3 can't refute) → 2 completeness critics. **Caught 1 HIGH + 2 MED that the green suite missed; correctly refuted 2 plausible-but-safe HIGH/MED.**
4. **Fix confirmed findings inline** (TDD: confirm-fail → fix → confirm-pass, commit-per-fix).
5. **Ship** (push `HEAD:main` + annotated tags).

## Track status (sequence LOCKED A→B→C, no majors this pass)
- **Track A — DONE** (plugin v4.8.1). Leftover: theme.json `caption`/`cite` element styles → fold into the first THEME minor.
- **Track B1 — DONE** (plugin v4.9.0 + v4.10.0/v4.10.1). "Operational hardening" fully shipped.
- **Track B — remaining (NOT yet grounded; ground before planning):**
  - **B2 · Plugin minor "Editor UX + frugal AI"** (5 items): body-ground the Insights advisor (frugal-AI ~$0.42/yr), pre-publish mistake gate (`PluginPrePublishPanel`), ⌘K palette expansion, AI release-notes drafter, insights "Create draft" button.
  - **B3 · Theme minor "Reader experience I"** (7): Related Notes footer (taxonomy heuristic), print/PDF stylesheet, "Updated DATE" line, copy-permalink/Web Share, Style Variations, brutalist block-style variations, editor palette curation — **+ the caption/cite carryover**.
  - **B4 · Theme minor "Feeds, blocks & pages"**: JSON Feed, reader ⌘K palette, `/colophon`+`/now`, RSS enrichment, PHP-registered sidenote/pull-quote blocks, Block Bindings, broken-link re-probe.
  - Theme work = new theme worktree off theme `main` (theme baseline 12 suites / 415). **Re-run a grounding workflow** (parallel agents → per-item implementation-ready specs) before planning each bundle — that's what made the implementers reliable.
- **Track C — NOT this pass** (per user): cleanup major (v5.0.0 + v10.0.0), the frugal-AI content-intelligence flagship, the Plausible seed (`signal-and-noise-tools/docs/superpowers/specs/2026-06-06-plausible-content-intelligence-seed.md`).

## Mechanics / gotchas
- **Plugin worktree** `signal-and-noise-tools/.claude/worktrees/v4.10.0` (branch `claude/v4.10.0`) is at the shipped tip — safe to `git worktree remove` once installs are confirmed. The v4.8.1/v4.9.0 worktrees are also removable.
- **phpcs `parallel=8` shows batches, not files** — falsify coverage with an injection or `phpcs -v --parallel=1`. `[[reference_phpcs_parallel_batches_not_files]]`.
- Build workflows can **stall** (the T6 implementer stalled 3× and a retry found its work uncommitted, then verified+committed it). Give them long timeouts; check the worktree state if a retry happened.
- Exclude `contracts-smoke.php` from local sweeps (WP-only).
- **Docs reconciled this session:** master-execution-sequence shipped-log + upgrade-opportunities-roadmap Track A/B1 status (this commit).
