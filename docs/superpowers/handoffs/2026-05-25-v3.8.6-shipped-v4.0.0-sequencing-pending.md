# Handoff — 2026-05-25 (v3.8.6 shipped, v4.0.0 sequencing pending)

**Hand off because:** context at 60% mid-conversation about sequencing remaining work. User wants a clean handoff while there's still budget. This session shipped **4 plugin releases** (v3.8.3 audit log + v3.8.4/5/6 follow-ups) plus the roadmap reconciliation. Plenty done; clean break point before sequencing the v4.0.0 ship.

---

## TL;DR — production state

| Surface | Latest tag | Patch cap | Minor cap |
|---|---|---|---|
| **Theme** | **v9.1.5** | 5/7 in v9.1.x — 2 patches remain before v9.2.0 rollover | 1/5 in v9.x |
| **Plugin** | **v3.8.6** | 6/7 in v3.8.x — **1 patch remains** before v3.9.0 rollover | 8/5 in v3.x — **PAST CAP** |

**Cap reality:** plugin minor count is past the 5/major cap (v3.0 → v3.8 is 9 minors). The next minor bump auto-rolls to **v4.0.0**. There is no v3.9.0 path per the existing project rules — bending the cap would need a deliberate one-time override in CLAUDE.md docs/VERSIONING.md.

**4 plugin ships this session** (in chronological order):

| # | Tag | What | Commit |
|---|---|---|---|
| 1 | **plugin v3.8.3** | Login hardening audit log shipped: Security → Audit log sub-tab; 6 captured events with 90-day retention; full 4-surface dispatch (admin/REST/4 abilities/2 ⌘K); +991 LOC across 8 files | `cabee72` |
| 2 | **plugin v3.8.4** | Fix: desktop-mode dock submenu derives from `sn_admin_top_tabs()` (was hardcoded 8 entries; single-source-of-truth violation from v3.8.1 sidebar reduction) | `48443ba` |
| 3 | **plugin v3.8.5** | Fix: `.sn-2col` always stacks (RSS tab was cramped at every viewport; v3.8.4's breakpoint bump 960→1200px insufficient on monitors >1200px) | `73eafd2` |
| 4 | **plugin v3.8.6** | Viewport-fit admin pages: sticky chrome (sub-tab nav + TOC) + `.snt-scroll-table` opt-in wrapper on 6 tables across 5 module files + hero card density + `--sn-space-4`/5 tightening rippling through 23 callsites | `ba4aec6` |

**Plus** 1 reconciliation handoff (committed earlier this session as `04f9b49`), 1 reconciliation update (`cc7cb60`), 2 design specs, 2 implementation plans, several memory entries.

User confirmed v3.8.6 visually: "It looks great."

---

## What's outstanding (for next session)

### 1. Sequence v3.8.x wrap-up → v4.0.0 ship — USER PRIORITY

User's exact words: *"Let's sequence everything... Do the things for wrap up 3.8.x, then the small jump to 3.9.x to go 4.0.0 afterwards. Maybe we need to use skills and all that to plan and streamline it."*

**Cap-mechanics flag:** I was mid-response explaining that v3.9.0 doesn't exist per the project's cap rules (9 minors already in v3.x; cap is 5). Two viable interpretations of the user's intent:

| Interpretation | What it means | Recommended? |
|---|---|---|
| **A. Strict cap (recommended)** | Wrap-up items batch into one final patch (**v3.8.7**); next minor ship is **v4.0.0** directly | ✅ Cleaner; respects existing rule |
| **B. Bend cap as one-time override** | Document a cap override in CLAUDE.md / docs/VERSIONING.md, ship transitional **v3.9.0** with small features, then v4.0.0 | ⚠️ Adds rule-bending docs; more ceremony |

**My intended response (interrupted):** propose Path A — single v3.8.7 wrap-up patch batching the 3 small residuals, then v4.0.0 = AI-assisted content-health fixes (the headline feature). Two phases, two ships. Use brainstorming + writing-plans skills per phase.

User to decide between A and B at session start.

### 2. Wrap-up items (whatever wrapper version they ship under)

From the [reconciliation handoff residual TODO](2026-05-25-roadmap-reconciliation.md):

| # | Item | Scope | Effort |
|---|---|---|---|
| a | **GSC sitemap submission** | Verify `/wp-sitemap.xml` is fetchable + user submits in Google Search Console | ~15 min (no code; mostly user-side config) |
| b | **Native Breadcrumbs theme adoption check** | Grep theme templates/patterns for `core/breadcrumbs` usage. Likely closes as no-action (no current usage — JSON-LD emission already covered by `seo-schema.php:sn_schema_breadcrumb_list()`) | ~30 min verification, likely 0 LOC |
| c | **WORDPRESS-REFERENCE.md gotchas append** | Add 3 lessons from this session arc to the running gotchas list in [docs/WORDPRESS-REFERENCE.md](../../WORDPRESS-REFERENCE.md): `get_file_data()` version pattern + `method_exists()` vs `__call` mismatch + desktop-mode horizontal-submenu warning | ~20 min docs only |

Batched into one ship: ~30-60 min total effort. Pure docs/verification + maybe a tiny code change if breadcrumbs check finds something. Could land as **v3.8.7** (final patch in v3.8.x) OR (Path B) as v3.9.0.

### 3. v4.0.0 ship — AI-assisted content-health fix proposals

The headline v4.0.0 feature. Current state:

- `inc/health-checks.php` v3.5.0 ships 4 detection-only checks: missing_alt, orphaned_media, broken_links, stale_posts
- `SNT_AI_DRIFT_SYSTEM` constant at health-checks.php:39-48 hints at AI drift detection but is the only AI surface present in that module today
- The module's docblock at line 14-15 explicitly says: *"AI-assisted fix proposals are a future extension"*

**Recommended scope per the reconciliation handoff:** missing-alt suggestions (one-click apply) + stale-post freshness rewrites. Both have a clear AI surface and a one-shot "accept" UX. Plus a possible third: orphaned-media decision helper.

**Estimated:** ~200-300 LOC, includes 2-3 new AI Abilities + REST endpoints + admin UI buttons in the Health tab. Real focused session of brainstorm → spec → plan → execute.

**v4.0.0-specific deliverables on top of the feature:**
- CHANGELOG headline "v4.0.0 — major release" treatment
- Brief release notes (what changed since v3.0.0; could be a single paragraph in CHANGELOG referencing the 36 v3.x ships)
- Document the cap rollover in CHANGELOG entry
- Optional: a memory entry capturing the v3.x → v4.x journey lessons

### 4. Browser verification of v3.8.6 (low priority, user-confirmed visually but...)

User said "It looks great" but didn't explicitly walk through every gate G1-G16 from the v3.8.6 spec. If anything breaks in real use, ship v3.8.7 as a fix instead of the wrap-up batch.

### 5. Potential memory entries to create (low priority, defer)

Worth capturing eventually but not blocking:

- **Sticky-chrome pattern memory** — "anchor sub-tab nav + TOC, data scrolls internally" as a reusable WP-admin pattern. Defer until used 2+ times.
- **Density-tightening discipline** — CSS-variable-tightening approach (change `--sn-space-X` once, ripples through N callsites). Defer until needed again.
- **Atomic CSS-coupled commit pattern** — v3.8.6 was an explicit "single commit because changes are visually coupled" decision. Could capture the heuristic.

---

## Process discipline summary (this session)

- **4 plugin releases shipped** in one session, all clean atomic commits, all tagged + pushed + deployed
- Used `superpowers:brainstorming` (twice: v3.8.3 audit log + v3.8.6 viewport-fit), `superpowers:writing-plans` (twice), `superpowers:executing-plans` (twice), inline execution mode both times
- Hard rule `feedback_skills_plugins_docs_always.md` followed: every coding task ran through the skill chain
- 2 follow-up patches (v3.8.4, v3.8.5) shipped fix-forward after surfaced regressions — small, isolated, atomic
- 1 cap-mechanics flag caught BEFORE the user sequenced v3.9.0/v4.0.0
- 0 force pushes, 0 amends, 0 `--no-verify` usage
- All commits signed with `Co-Authored-By: Claude Opus 4.7 (1M context)`

**Cumulative lesson reinforced:** the single-source-of-truth pattern bit us TWICE in different contexts this arc (v3.8.2 SNT_VERSION drift + v3.8.4 dock submenu drift). Memory `feedback_version_constants_must_derive_from_docblock.md` was generalized to cover both. *Active hunt for two-sources-of-truth patterns is now part of code review discipline.*

---

## Key file locations (for fresh-session reload)

| What | Path |
|---|---|
| **This handoff** | `docs/superpowers/handoffs/2026-05-25-v3.8.6-shipped-v4.0.0-sequencing-pending.md` (you're reading it) |
| Prior handoff (session start) | `docs/superpowers/handoffs/2026-05-25-sub-tabs-arc-complete.md` |
| Prior handoff (mid-session) | `docs/superpowers/handoffs/2026-05-25-roadmap-reconciliation.md` (updated with session addendum) |
| v3.8.3 audit log spec | `signal-and-noise-tools/docs/superpowers/specs/2026-05-25-login-hardening-audit-log-design.md` |
| v3.8.3 audit log plan | `signal-and-noise-tools/docs/superpowers/plans/2026-05-25-login-hardening-audit-log-v3.8.3.md` |
| v3.8.6 viewport-fit spec | `signal-and-noise-tools/docs/superpowers/specs/2026-05-25-viewport-fit-admin-pages-design.md` |
| v3.8.6 viewport-fit plan | `signal-and-noise-tools/docs/superpowers/plans/2026-05-25-viewport-fit-admin-pages-v3.8.6.md` |
| Roadmap (still relevant for v4.0.0) | `signal-and-noise/docs/superpowers/specs/2026-05-16-plugin-absorption-roadmap.md` (has reconciliation banner pointing to the reconciliation handoff) |
| Health checks module (v4.0.0 target) | `signal-and-noise-tools/inc/health-checks.php` (detection-only impls + `SNT_AI_DRIFT_SYSTEM` constant) |
| Health checks admin tab (v4.0.0 surface) | `signal-and-noise-tools/inc/health-checks-admin.php` |
| AI bootstrap (reusable for v4.0.0) | `signal-and-noise-tools/inc/ai-bootstrap.php` (`snt_ai_can_text_generate()` gate + `snt_ai_generate_with_constraints()` helper) |

---

## Memory updates this session

- ✅ Created: `feedback_version_constants_must_derive_from_docblock.md` (then generalized with v3.8.4 reinforcement)
- ✅ Created: `feedback_desktop_mode_horizontal_submenu_warning.md`
- ✅ Created: `feedback_internal_toc_vs_sub_tabs_decision.md`
- ✅ Updated: `project_architecture.md` (current state: theme v9.1.5 + plugin v3.8.5; **slightly stale** — needs bump to plugin v3.8.6 next session)
- ✅ Updated: `MEMORY.md` index

---

## Recommended next-session sequence

1. **Read this handoff** (you're doing it)
2. **Confirm production state still matches** (1 SSH command):
   ```bash
   ssh -i ~/.ssh/cloudways_deploy master_syguxtyfsh@157.245.116.64 \
     'php -r "include \"/home/master/applications/nffqxsrgxz/public_html/wp-load.php\"; echo SNT_VERSION;"'
   ```
   Should return `3.8.6`.
3. **Update project_architecture.md memory** — bump current state from plugin v3.8.5 → v3.8.6 (5 min docs work; do this first to avoid carrying stale memory into the v4.0.0 planning).
4. **Get user's call on cap-mechanics:** Path A (strict cap, v3.8.7 then v4.0.0) vs. Path B (bend cap, v3.9.0 transitional). My recommendation is Path A; user may pick differently.
5. **Phase 1 — wrap-up patch (v3.8.7 if Path A):**
   - Brainstorm scope (or skip brainstorm since the 3 items are well-scoped)
   - Skip-to-execution: GSC sitemap fetchability check (curl + flag for user) + breadcrumbs grep (likely no-action) + WORDPRESS-REFERENCE.md gotchas append
   - Single docs-mostly patch
   - Ship if Path A; ship as v3.9.0 if Path B
6. **Phase 2 — v4.0.0 ship — AI-assisted content-health fixes:**
   - Full skill chain: brainstorming → writing-plans → executing-plans
   - Headline feature: missing-alt suggestions (one-click apply) + stale-post freshness rewrites + (optional) orphaned-media decision helper
   - v4.0.0 release notes treatment in CHANGELOG (acknowledge the v3.x → v4.x rollover)
   - Document cap rollover in CHANGELOG entry
   - Optional memory entry capturing v3.x → v4.x lessons
7. **Consider creating the 3 deferred memory entries** flagged in Section 5 above (each prevents recurrence of a pattern from this session arc).

---

## Open questions for next session

1. **Path A vs Path B** on cap mechanics — user picks
2. **v4.0.0 feature scope** — confirm missing-alt + stale-posts as the v4.0.0 headline, OR adjust scope based on user priority
3. **Browser verification depth** — did the user actually walk through all 16 v3.8.6 gates, or was "looks great" a quick top-of-page check? Worth confirming before assuming all gates pass.

---

## One-line summary

**4 plugin ships this session (v3.8.3 audit log + v3.8.4/5/6 fixes & viewport-fit). Theme v9.1.5 + plugin v3.8.6 live; user said "It looks great." 1 patch remains in v3.8.x; next minor bump auto-rolls to v4.0.0 (cap at 5/major, currently 8/5). User wants sequenced wrap-up → v4.0.0; cap mechanics flagged. Next session: pick Path A (v3.8.7 wrap-up patch + v4.0.0 AI feature) vs Path B (bend cap, v3.9.0 transitional), then phase 1 + phase 2 through full skill chain.**
