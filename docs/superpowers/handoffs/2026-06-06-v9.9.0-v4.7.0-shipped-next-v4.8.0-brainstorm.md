# Handoff — v9.9.0 + v4.7.0 shipped; NEXT = brainstorm plugin v4.8.0 (2026-06-06)

**Read this first.** Context was cleared after a long multi-release session. The next task is a **brainstorm**, not a build.

## ⭐ TOP: next task = brainstorm plugin **v4.8.0** (post-editor sidebar consolidation + AI prepopulation)

The user chose to build the plugin toward ~v4.9.0; v4.7.0 shipped, and **v4.8.0 is next** — but it's a *fuzzy* stub with open product/UX decisions that MUST be brainstormed with the user before any code. **Do NOT autonomously guess these.**

- **Stub (start here):** `signal-and-noise-tools` repo → `docs/superpowers/specs/2026-05-27-v4.8.0-post-editor-consolidation-stub.md` (read it fully). Local plugin checkout: `/Users/juanlentino/Projects/signal-and-noise-tools` (on `main`, v4.7.0).
- **Two workstreams in the stub:** (A) **sidebar cleanup** — dedupe AI-generation surfaces between the `ai/ai` plugin and SN (primary: meta description appears twice); (B) **AI prepopulation** — auto-fill OG card title + meta description + excerpt at `draft → publish/scheduled`, empty-field-only, "max 3 short sentences."
- **Open decisions to resolve in the brainstorm:**
  1. **Cleanup ambition:** 3.1 (hide `ai/ai`'s duplicate meta-desc panel, ~30 LOC, lowest risk) vs 3.2 (redirect `ai/ai`'s write into SN's `sn_meta_description` meta) vs 3.3 (consolidate ALL AI buttons into one SN panel, ~200 LOC, highest risk). Stub recommends starting at 3.1.
  2. **Prepopulation scope:** ship cleanup only, prepop only, or both in v4.8.0? (~380–600 LOC range.)
  3. **`concise` param + length budgets** for `ai-generate-meta-description` (≤160 chars) + `ai-generate-excerpt` (~40–60 words, ≤3 sentences).
  4. **Admin-notice UX** for auto-generated fields ("auto-generated when you published — Edit/Regenerate").
- **PREREQUISITE (do during brainstorm/plan, not before):** **source-audit the `ai/ai` plugin** — find the meta-desc panel slug (for `remove_meta_box`/filter) + whether any filter allows write-redirection. The `ai/ai` plugin is installed; see memory `reference_ai_plugin_v1_features.md` (it predicted this exact duplication).
- **Process:** invoke `superpowers:brainstorming` → settle the above → write plan → build → adversarial review → ship. Same rigor as v4.6.0/v4.7.0.

## What shipped THIS session (all on `main`, all tagged)
All built with: subagent-driven / one-implementer-per-cycle → **adversarial-review workflow** (refute-by-default) → fix → verify. Lesson reinforced repeatedly: **read the real impl contract + write BEHAVIORAL tests** (see memory `feedback_verify_impl_contracts_behavioral_tests`).

| Release | Commit | Tag | What | Review outcome |
|---|---|---|---|---|
| theme **v9.8.0** | (earlier this session) | `v9.8.0` | Notes-scoped search & archive (resolved the v9.7.0 header "black blob") | 6 findings fixed; live-verified |
| **A0 gate** | `e9488c9` | — | WP 7.0 confirmed live on Cloudways; v4.6.0/v5.0.0 plan drift reconciled | — |
| plugin **v4.6.0** | `57dbe39` | `v4.6.0` | 6 new abilities (→40) + WP<7.0 notice + `@deprecated` on 6 REST handlers | **15 confirmed (2 HIGH) fixed** — dead dismiss store, scan envelope, unregistered `'tools'` category, weaker cron dispatcher |
| theme **v9.9.0** | `8bba8db` | `v9.9.0` | `get-latest-theme-tag` ability (→13) + WP<7.0 notice; prep for v10.0.0 | 1 finding fixed (ability annotations) |
| plugin **v4.7.0** | `af4900b` | `v4.7.0` | Admin-bar expansion: 3 quick-actions (Force Update Check, Scan Pattern Adoption, Regen OG Card contextual) | **CLEAN** (no IDOR) |

Current versions: **theme v9.9.0, plugin v4.7.0.** Gates at session end: theme 12 suites/415 assertions, plugin 34 suites/1146 assertions, **0 failures**; phpcs clean (falsification-confirmed) in both.

## ⚠ Pending user installs (shipped to main+tagged, NOT auto-deployed)
Tag push does NOT auto-deploy either repo. Install via **wp-admin → Dashboard → Updates** ("Update theme" / "Update plugin"):
- **theme v9.9.0** — pending.
- **plugin v4.6.0 + v4.7.0** — pending (user installed up through v4.5.x / v9.8.0 earlier; confirm the rest).
- Both WP<7.0 admin notices are correctly SUPPRESSED on the live WP 7.0 site (by design — no UAT for them). The plugin v4.7.0 admin-bar actions DO appear in the **S&N ▾** dropdown (Force Update Check + Scan always; Regen OG Card only on post-edit screens).

## Deferred / still queued (after v4.8.0)
- **Toward ~v4.9.0:** v4.8.0 is the last rung the user named; reassess after it ships.
- **Deferred from v4.6.0:** WS7 — GitHub rate-monitor per-caller split (`inc/api-rate-monitor.php`); was NOT in the v4.6.0 task breakdown. → v4.6.x or fold into a later minor.
- **Major chain:** v5.0.0 BC → **plugin v5.0.0** (WP 7.0 hard-raise + remove 10 deprecated REST routes + JS→Ability flip + orphan-option) **paired with theme v10.0.0**. v5.0.0 Task-7 already rescoped to warn `sn_admin_pages()` (A0 reconcile). Both prep-minors (v4.6.0, v9.9.0) are done.
- **Frontier candidates (independent, each needs brainstorm→plan):** helpful 404 recovery (smallest, both repos), in-article TOC + progress rail (theme), IndexNow (plugin).
- **`/notes` R2** (plugin: `sn_notes_per_page` setting + paged SEO) — watch the title double-append (theme owns `pre_get_document_title` for `/notes`).

## Working context
- **Theme worktree:** `/Users/juanlentino/Projects/signal-and-noise/.claude/worktrees/nice-goldstine-063551` (branch `claude/nice-goldstine-063551`, == `main`). Push via `git push origin HEAD:main`.
- **Plugin:** `/Users/juanlentino/Projects/signal-and-noise-tools` (on `main`, v4.7.0). Build v4.8.0 in a fresh worktree (`.claude/worktrees/v4.8.0`); `composer install` in the worktree for phpcs (vendor isn't copied). `contracts-smoke.php` is a WP-only test — CI-excluded; exclude it from local sweeps too.
- Canonical roadmap: `docs/superpowers/specs/2026-06-05-master-execution-sequence.md` (theme repo) — updated through v4.7.0.
